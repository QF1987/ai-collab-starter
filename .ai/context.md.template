# <PROJECT_NAME> Context

> Bootstrap 时由 Claude 基于项目实际填充本文件，之后保持与真实代码状态对齐。
> 文件名提示：填好后请将 `context.md.template` 改名为 `context.md`。

## Project Identity

`<PROJECT_NAME>` 是 `<一句话定位：做什么 / 解决什么问题 / 服务什么用户>`。

本项目使用多 Agent 协同框架（详见 `AGENTS.md`）。本文件用于 Agent 在每次开始任务前理解项目当前状态。

## Repo Map

> 列出本项目涉及的所有 git 仓库 / 子模块 / 关键目录。
> 单仓项目此节为 1 行；多仓项目按角色分行。

| Role | Path | 说明 |
| --- | --- | --- |
| <main> | `<REPO_PATH>` | <主仓库 / 单仓项目> |
| <若多仓> | `<REPO_X_PATH>` | <角色描述> |

## Key Boundaries

> 项目主要架构边界（API / 协议 / 数据契约）。
> 详细的架构分层和评审触发条件在 `architecture.md`。

- 主边界类型: <gRPC / REST API / Message Queue / File Protocol / etc.>
- 主要接口: <列出 1-3 个核心接口>
- 跨边界变更纪律: <例如 proto 双仓同步 / OpenAPI 双端更新>

## Current State

> 本项目当前进度快照。每次重大里程碑后由 Claude 或人更新本节。
> Bootstrap 时此节可只填一行「项目初始化，尚无完成里程碑」。

### Recent commits / milestones

- `<hash>` — `<commit subject>` (`<date>`)
- ...

### Status by milestone / epic

| 标签 | 内容 | 状态 | 证据 |
| --- | --- | --- | --- |
| <M1> | <一句话目标> | ✅ / ⏳ / ❌ | `<commit hash>` / 测试输出引用 |

## Active Review Items

> 当前未关闭的 review findings 简单清单（详细在 `review.md`）。

- 无（或列 RV-YYYYMMDD-NN: 一句话 + status）

## What's Next

> 按优先级排序的下一步可执行任务清单。
> 由 intake skill / Claude plan / 人 显式更新。

| 顺序 | 标签 | 内容 | Repo | 关键文件 | 预估 |
| --- | --- | --- | --- | --- | --- |
| 1 | <P0/P1/M2-A 等> | <一句话> | <repo> | <path> | <时长> |

---

## Bootstrap 备注

本文件初稿由 Claude bootstrap session 生成。**人审后必须做的事**：

1. 删除本「Bootstrap 备注」段
2. 把文件改名 `context.md.template` → `context.md`
3. 把所有 `<...>` 占位符替换为实际内容
4. 检查 Repo Map 路径都真实存在
5. 把首个真实任务的状态填到 What's Next 表
